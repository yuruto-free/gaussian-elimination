# ガウスの消去法
ガウスの消去法をC言語で実装する。

## 環境
* コンパイラ

    GCC

* ライブラリ

    math.h

* 必要なコマンド

    make

## ビルド
以下のコマンドを実行する。

```sh
make
```

## 実行
以下のコマンドを実行する。

```sh
./gaussian-elimination
```

## 実行ファイルの削除
以下のコマンドを実行する。

```sh
make clean
```